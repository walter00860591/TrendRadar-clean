name: Smoke Test

# Minimal workflow used to verify Actions runner availability.

on:
  workflow_dispatch:

jobs:
  smoke-test:
    runs-on: ubuntu-latest
    steps:
      - name: Print a test message
        run: echo "Smoke test is running"
